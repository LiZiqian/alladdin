# 架构整理与交接验证记录（2026-09-22）

本轮基于 `Astra_920` 当时的完整工作树，保留开始前已有的功能和 UI 改动，没有回退到 Git 提交版本。版本号、数据库结构、业务 API 和 CSS 未变。未提交、推送或发布。

## 改动范围

| 原文件 | 整理前 | 整理后 | 实现归属 |
| --- | ---: | ---: | --- |
| `frontend/js/app.server.js` | 2,241 行 | 311 行 | 同步协调入口 + `server/` 下 9 个职责模块。 |
| `frontend/js/samples/01-pool.js` | 1,553 行 | 685 行 | 列表/卡片 + 独立的分页、销毁、新增模块。 |
| `backend/server_modules/mutation_services.py` | 1,925 行 | 25 行 | 稳定导出入口 + `mutations/` 下公共工具、校验、副作用和业务服务。 |

这些数字表示文件职责拆分，不表示删除了同等行数的业务实现。新的通信模块均小于 300 行，新的后端服务模块最多 421 行。文件头、关键状态和事务边界补了说明；原有业务注释保留。

实际精简包括：13 个查询入口共用 JSON 应答校验、通用分页查询串构造集中、6 处相同的写入成功确认集中。移除没有生产加载或调用的空占位文件 `frontend/js/samples.js`。历史兼容分支和无法证明无用的函数没有按名称猜测删除。

增加 [架构导航](architecture.md)、[维护指南](maintenance.md)、[测试说明](../tests/README.md) 和 `scripts/check.py`。新增文件应与入口加载清单一起交付。`output/` 与 `.playwright-cli/` 已加入忽略规则，避免把测试快照和浏览器缓存误当源码提交。

本机 `.gitignore` 自身被 `.git/info/exclude` 忽略，因此忽略规则目前仅在本地生效；后续正式提交需显式审阅纳入，详见维护指南。

## 验证结果

- **当前交付范围 88/88 检查通过**：44 套回归 + Python/JavaScript 语法检查。
- 另外运行全部本机历史测试后为 **95/104 通过**，剩余 9 套均在重构基线中已失败；没有新增失败套件。不能把这个包含历史测试的结果写成“全部通过”。
- 27 个后端函数/类定义去除文档字符串后，AST 与基线一致；流程允许写入的样机字段白名单一致。
- 96 个原前端通信方法全部保留，其中 77 个函数体仅规范换行后逐字一致；另外 19 个只涉及上述查询和成功确认去重。样机池原有 **67 个方法全部保留且函数体一致**。
- 28 个 CSS/相关资源文件与基线字节一致；HTML 页面外壳仅增加脚本引用，其他内容一致。
- 同一合成数据下，11 个真实 HTTP 读取接口的完整 JSON 应答与基线一致，覆盖 bootstrap、项目、任务分页、池、样机分页、候选、照片、事件、履历、销毁影响范围。
- 实际浏览器在 **1280×800** 下比较首页、样机分页、样机详情、项目工作台、任务配置、样机选择器，六组截图的 SHA-256 完全相同。
- 通过浏览器编辑合成样机备注、保存、刷新页面、重新打开详情，确认备注持久化；观察窗口内没有浏览器 warning/error。
- `git diff --check` 通过。

当前回归中增加了生产脚本清单加载、成员覆盖、查询空值/日期语义、HTTP 错误、失败不重试、写入确认顺序和拆分模块全局依赖检查。三套原先缺附件模块的详情测试及一套状态测试补齐了真实模块依赖。

### 历史失败的具体边界

| 历史测试 | 原因 |
| --- | --- |
| `frontend_access_control`、`frontend_sample_access_context`、`test_access_control`、`test_access_policy_transfer` | 当前工作树没有历史 ACL 实现。 |
| `frontend_architecture_guard` | 既有页面已存在分散的 innerHTML 写入，与旧集中化断言不同。新生产加载合同另有回归，未放宽旧断言。 |
| `frontend_dom_helpers` | 问题表新增列后，旧测试仍按旧列序读取；已补真实照片模块，但保留原失败断言。 |
| `frontend_project_boundary`、`frontend_workspace_visual_contract` | 旧颜色/布局断言与当前 UI 不匹配。没有为它们改变现有 UI。 |
| `test_architecture_guards` | 本机 AGENTS 行号表过时；历史行号表不是本轮交接导航，新的导航采用文件与函数职责。 |

## 本地证据

原始源码快照、完整测试日志和截图位于本机 `output/architecture-refactor-20260922/`，该目录不随 Git 源码交付：

- `baseline/`、`baseline-manifest.json`、`starting-status.txt`：开始时的源码基线，不含正式业务数据。
- `baseline/results.json`、`final/results.json`：基线与最终结果，每项均有独立日志。
- `source-comparison.json`、`frontend-method-comparison.json`：定义/函数体及资源比较。
- `http-read-comparison.json`：11 个 HTTP 应答比较。
- `browser/*-fixed-before.png`、`browser/*-fixed-after.png`：六组统一视口截图；`browser/comparisons.json` 也保留早期视口不同的非等价比较，未删除失败记录。
- `browser/saved-note.json`：刷新后备注读取结果。

首次检查脚本曾给所有子进程注入临时 `TESTCHAMBER_DATA_DIR`，干扰两套专门测试“默认路径”的测试。已修正脚本并重新取得基线；原尝试保存在 `before/`，不用于最终对比。首次拆分回归发现字段白名单遗漏，已按基线补回，并增加全局依赖检查；失败日志同样保留。

## 仍需后续维护的内容

结果录入、数据规范化、迁移提交仍包含较复杂业务；本轮没有声称清除了项目全部历史问题。后续继续按领域分批整理，用实际数据约束与回归证明可以删除的分支。发布 workflow 的旧版本线和 ACL 是否恢复属于独立决策，本轮没有顺带改变。
